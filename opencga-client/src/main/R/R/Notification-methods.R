
# WARNING: AUTOGENERATED CODE
#
#    This code was generated by a tool.
#    
#    Manual changes to this file may cause unexpected behavior in your application.
#    Manual changes to this file will be overwritten if the code is regenerated.


# ##############################################################################
#' NotificationClient methods
#' @include AllClasses.R
#' @include AllGenerics.R
#' @include commons.R

#' @description This function implements the OpenCGA calls for managing Notifications.

#' The following table summarises the available *actions* for this client:
#'
#' | endpointName | Endpoint WS | parameters accepted |
#' | -- | :-- | --: |
#' | aggregationStats | /{apiVersion}/notifications/aggregationStats | organization, uuid, type, scope, fqn, sender, target, receiver, internalStatus, creationDate, modificationDate, field |
#' | create | /{apiVersion}/notifications/create | body[*] |
#' | search | /{apiVersion}/notifications/search | include, exclude, limit, skip, count, organization, uuid, type, scope, fqn, sender, target, receiver, internalStatus, creationDate, modificationDate |
#' | info | /{apiVersion}/notifications/{notification}/info | include, exclude, notification[*], organization |
#'
#' @md
#' @seealso \url{http://docs.opencb.org/display/opencga/Using+OpenCGA} and the RESTful API documentation
#' \url{http://bioinfo.hpc.cam.ac.uk/opencga-prod/webservices/}
#' [*]: Required parameter
#' @export

setMethod("notificationClient", "OpencgaR", function(OpencgaR, notification, endpointName, params=NULL, ...) {
    switch(endpointName,

        #' @section Endpoint /{apiVersion}/notifications/aggregationStats:
        #' Fetch catalog notification stats.
        #' @param organization Organization id.
        #' @param uuid Notification UUID.
        #' @param type Notification type: INFO, WARNING, ERROR or URGENT.
        #' @param scope Notification scope: GLOBAL, ORGANIZATION, PROJECT or STUDY.
        #' @param fqn Organization, project or study fqn where the notification is associated.
        #' @param sender User ID of the sender.
        #' @param target Target of the notification.
        #' @param receiver User ID receiver of the notification.
        #' @param internalStatus Notification status: PENDING, SUCCESS, ERROR or DISCARDED.
        #' @param creationDate Creation date. Format: yyyyMMddHHmmss. Examples: >2018, 2017-2018, <201805.
        #' @param modificationDate Modification date. Format: yyyyMMddHHmmss. Examples: >2018, 2017-2018, <201805.
        #' @param field Field to apply aggregation statistics to (or a list of fields separated by semicolons), e.g.: studies;type;numSamples[0..10]:1;format:sum(size).
        aggregationStats=fetchOpenCGA(object=OpencgaR, category="notifications", categoryId=NULL, subcategory=NULL,
                subcategoryId=NULL, action="aggregationStats", params=params, httpMethod="GET", as.queryParam=NULL,
                ...),

        #' @section Endpoint /{apiVersion}/notifications/create:
        #' Create a new notification.
        #' @param data JSON containing the notification information.
        create=fetchOpenCGA(object=OpencgaR, category="notifications", categoryId=NULL, subcategory=NULL,
                subcategoryId=NULL, action="create", params=params, httpMethod="POST", as.queryParam=NULL, ...),

        #' @section Endpoint /{apiVersion}/notifications/search:
        #' Notification search method.
        #' @param include Fields included in the response, whole JSON path must be provided.
        #' @param exclude Fields excluded in the response, whole JSON path must be provided.
        #' @param limit Number of results to be returned.
        #' @param skip Number of results to skip.
        #' @param count Get the total number of results matching the query. Deactivated by default.
        #' @param organization Organization id.
        #' @param uuid Notification UUID.
        #' @param type Notification type: INFO, WARNING, ERROR or URGENT.
        #' @param scope Notification scope: GLOBAL, ORGANIZATION, PROJECT or STUDY.
        #' @param fqn Organization, project or study fqn where the notification is associated.
        #' @param sender User ID of the sender.
        #' @param target Target of the notification.
        #' @param receiver User ID receiver of the notification.
        #' @param internalStatus Notification status: PENDING, SUCCESS, ERROR or DISCARDED.
        #' @param creationDate Creation date. Format: yyyyMMddHHmmss. Examples: >2018, 2017-2018, <201805.
        #' @param modificationDate Modification date. Format: yyyyMMddHHmmss. Examples: >2018, 2017-2018, <201805.
        search=fetchOpenCGA(object=OpencgaR, category="notifications", categoryId=NULL, subcategory=NULL,
                subcategoryId=NULL, action="search", params=params, httpMethod="GET", as.queryParam=NULL, ...),

        #' @section Endpoint /{apiVersion}/notifications/{notification}/info:
        #' Return the notification information.
        #' @param include Fields included in the response, whole JSON path must be provided.
        #' @param exclude Fields excluded in the response, whole JSON path must be provided.
        #' @param notification Notification UUID.
        #' @param organization Organization id.
        info=fetchOpenCGA(object=OpencgaR, category="notifications", categoryId=notification, subcategory=NULL,
                subcategoryId=NULL, action="info", params=params, httpMethod="GET", as.queryParam=NULL, ...),
    )
})