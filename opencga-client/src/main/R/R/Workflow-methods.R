
# WARNING: AUTOGENERATED CODE
#
#    This code was generated by a tool.
#    
#    Manual changes to this file may cause unexpected behavior in your application.
#    Manual changes to this file will be overwritten if the code is regenerated.


# ##############################################################################
#' WorkflowClient methods
#' @include AllClasses.R
#' @include AllGenerics.R
#' @include commons.R

#' @description This function implements the OpenCGA calls for managing Workflows.

#' The following table summarises the available *actions* for this client:
#'
#' | endpointName | Endpoint WS | parameters accepted |
#' | -- | :-- | --: |
#' | updateAcl | /{apiVersion}/workflows/acl/{members}/update | study, members[*], action[*], body[*] |
#' | create | /{apiVersion}/workflows/create | include, exclude, study, includeResult, body[*] |
#' | distinct | /{apiVersion}/workflows/distinct | study, id, name, uuid, tags, draft, internal.registrationUserId, type, creationDate, modificationDate, acl, release, snapshot, deleted, field[*] |
#' | run | /{apiVersion}/workflows/run | study, jobId, jobDescription, jobDependsOn, jobTags, jobScheduledStartTime, jobPriority, jobDryRun, body[*] |
#' | search | /{apiVersion}/workflows/search | include, exclude, limit, skip, count, study, id, name, uuid, tags, draft, internal.registrationUserId, type, creationDate, modificationDate, acl, release, snapshot, deleted |
#' | update | /{apiVersion}/workflows/{workflowId}/update | include, exclude, workflowId[*], study, includeResult, body |
#' | acl | /{apiVersion}/workflows/{workflows}/acl | workflows[*], study, member, silent |
#' | delete | /{apiVersion}/workflows/{workflows}/delete | study, workflows[*] |
#' | info | /{apiVersion}/workflows/{workflows}/info | include, exclude, workflows[*], study, version, deleted |
#'
#' @md
#' @seealso \url{http://docs.opencb.org/display/opencga/Using+OpenCGA} and the RESTful API documentation
#' \url{http://bioinfo.hpc.cam.ac.uk/opencga-prod/webservices/}
#' [*]: Required parameter
#' @export

setMethod("workflowClient", "OpencgaR", function(OpencgaR, members, workflowId, workflows, endpointName, params=NULL, ...) {
    switch(endpointName,

        #' @section Endpoint /{apiVersion}/workflows/acl/{members}/update:
        #' Update the set of permissions granted for the member.
        #' @param study Study [[organization@]project:]study where study and project can be either the ID or UUID.
        #' @param members Comma separated list of user or group ids.
        #' @param action Action to be performed [ADD, SET, REMOVE or RESET]. Allowed values: ['SET ADD REMOVE RESET']
        #' @param data JSON containing the parameters to update the permissions.
        updateAcl=fetchOpenCGA(object=OpencgaR, category="workflows", categoryId=NULL, subcategory="acl",
                subcategoryId=members, action="update", params=params, httpMethod="POST", as.queryParam=c("action"),
                ...),

        #' @section Endpoint /{apiVersion}/workflows/create:
        #' Create a workflow.
        #' @param include Fields included in the response, whole JSON path must be provided.
        #' @param exclude Fields excluded in the response, whole JSON path must be provided.
        #' @param study Study [[organization@]project:]study where study and project can be either the ID or UUID.
        #' @param includeResult Flag indicating to include the created or updated document result in the response.
        #' @param data JSON containing workflow information.
        create=fetchOpenCGA(object=OpencgaR, category="workflows", categoryId=NULL, subcategory=NULL,
                subcategoryId=NULL, action="create", params=params, httpMethod="POST", as.queryParam=NULL, ...),

        #' @section Endpoint /{apiVersion}/workflows/distinct:
        #' Workflow distinct method.
        #' @param study Study [[organization@]project:]study where study and project can be either the ID or UUID.
        #' @param id Comma separated list of workflow IDs up to a maximum of 100. Also admits basic regular expressions using the operator '~', i.e. '~{perl-regex}' e.g. '~value' for case sensitive, '~/value/i' for case insensitive search.
        #' @param name Comma separated list of workflow names up to a maximum of 100. Also admits basic regular expressions using the operator '~', i.e. '~{perl-regex}' e.g. '~value' for case sensitive, '~/value/i' for case insensitive search.
        #' @param uuid Comma separated list of workflow UUIDs up to a maximum of 100.
        #' @param tags Comma separated list of tags.
        #' @param draft Boolean field indicating whether the workflow is a draft or not.
        #' @param internal.registrationUserId UserId that created the workflow.
        #' @param type Workflow type. Allowed types: [CLINICAL_INTERPRETATION, SECONDARY_ANALYSIS, RESEARCH or OTHER].
        #' @param creationDate Creation date. Format: yyyyMMddHHmmss. Examples: >2018, 2017-2018, <201805.
        #' @param modificationDate Modification date. Format: yyyyMMddHHmmss. Examples: >2018, 2017-2018, <201805.
        #' @param acl Filter entries for which a user has the provided permissions. Format: acl={user}:{permissions}. Example: acl=john:WRITE,WRITE_ANNOTATIONS will return all entries for which user john has both WRITE and WRITE_ANNOTATIONS permissions. Only study owners or administrators can query by this field. .
        #' @param release Release when it was created.
        #' @param snapshot Snapshot value (Latest version of the entry in the specified release).
        #' @param deleted Boolean to retrieve deleted entries.
        #' @param field Comma separated list of fields for which to obtain the distinct values.
        distinct=fetchOpenCGA(object=OpencgaR, category="workflows", categoryId=NULL, subcategory=NULL,
                subcategoryId=NULL, action="distinct", params=params, httpMethod="GET", as.queryParam=c("field"), ...),

        #' @section Endpoint /{apiVersion}/workflows/run:
        #' Execute a Nextflow analysis.
        #' @param study Study [[organization@]project:]study where study and project can be either the ID or UUID.
        #' @param jobId Job ID. It must be a unique string within the study. An ID will be autogenerated automatically if not provided.
        #' @param jobDescription Job description.
        #' @param jobDependsOn Comma separated list of existing job IDs the job will depend on.
        #' @param jobTags Job tags.
        #' @param jobScheduledStartTime Time when the job is scheduled to start.
        #' @param jobPriority Priority of the job.
        #' @param jobDryRun Flag indicating that the job will be executed in dry-run mode. In this mode, OpenCGA will validate that all parameters and prerequisites are correctly set for successful execution, but the job will not actually run.
        #' @param data NextFlow run parameters.
        run=fetchOpenCGA(object=OpencgaR, category="workflows", categoryId=NULL, subcategory=NULL, subcategoryId=NULL,
                action="run", params=params, httpMethod="POST", as.queryParam=NULL, ...),

        #' @section Endpoint /{apiVersion}/workflows/search:
        #' Workflow search method.
        #' @param include Fields included in the response, whole JSON path must be provided.
        #' @param exclude Fields excluded in the response, whole JSON path must be provided.
        #' @param limit Number of results to be returned.
        #' @param skip Number of results to skip.
        #' @param count Get the total number of results matching the query. Deactivated by default.
        #' @param study Study [[organization@]project:]study where study and project can be either the ID or UUID.
        #' @param id Comma separated list of workflow IDs up to a maximum of 100. Also admits basic regular expressions using the operator '~', i.e. '~{perl-regex}' e.g. '~value' for case sensitive, '~/value/i' for case insensitive search.
        #' @param name Comma separated list of workflow names up to a maximum of 100. Also admits basic regular expressions using the operator '~', i.e. '~{perl-regex}' e.g. '~value' for case sensitive, '~/value/i' for case insensitive search.
        #' @param uuid Comma separated list of workflow UUIDs up to a maximum of 100.
        #' @param tags Comma separated list of tags.
        #' @param draft Boolean field indicating whether the workflow is a draft or not.
        #' @param internal.registrationUserId UserId that created the workflow.
        #' @param type Workflow type. Allowed types: [CLINICAL_INTERPRETATION, SECONDARY_ANALYSIS, RESEARCH or OTHER].
        #' @param creationDate Creation date. Format: yyyyMMddHHmmss. Examples: >2018, 2017-2018, <201805.
        #' @param modificationDate Modification date. Format: yyyyMMddHHmmss. Examples: >2018, 2017-2018, <201805.
        #' @param acl Filter entries for which a user has the provided permissions. Format: acl={user}:{permissions}. Example: acl=john:WRITE,WRITE_ANNOTATIONS will return all entries for which user john has both WRITE and WRITE_ANNOTATIONS permissions. Only study owners or administrators can query by this field. .
        #' @param release Release when it was created.
        #' @param snapshot Snapshot value (Latest version of the entry in the specified release).
        #' @param deleted Boolean to retrieve deleted entries.
        search=fetchOpenCGA(object=OpencgaR, category="workflows", categoryId=NULL, subcategory=NULL,
                subcategoryId=NULL, action="search", params=params, httpMethod="GET", as.queryParam=NULL, ...),

        #' @section Endpoint /{apiVersion}/workflows/{workflowId}/update:
        #' Update some workflow attributes.
        #' @param include Fields included in the response, whole JSON path must be provided.
        #' @param exclude Fields excluded in the response, whole JSON path must be provided.
        #' @param workflowId Comma separated list workflow IDs or UUIDs up to a maximum of 100.
        #' @param study Study [[organization@]project:]study where study and project can be either the ID or UUID.
        #' @param includeResult Flag indicating to include the created or updated document result in the response.
        #' @param data body.
        update=fetchOpenCGA(object=OpencgaR, category="workflows", categoryId=workflowId, subcategory=NULL,
                subcategoryId=NULL, action="update", params=params, httpMethod="POST", as.queryParam=NULL, ...),

        #' @section Endpoint /{apiVersion}/workflows/{workflows}/acl:
        #' Returns the acl of the workflows. If member is provided, it will only return the acl for the member.
        #' @param workflows Comma separated list workflow IDs or UUIDs up to a maximum of 100.
        #' @param study Study [[organization@]project:]study where study and project can be either the ID or UUID.
        #' @param member User or group id.
        #' @param silent Boolean to retrieve all possible entries that are queried for, false to raise an exception whenever one of the entries looked for cannot be shown for whichever reason.
        acl=fetchOpenCGA(object=OpencgaR, category="workflows", categoryId=workflows, subcategory=NULL,
                subcategoryId=NULL, action="acl", params=params, httpMethod="GET", as.queryParam=NULL, ...),

        #' @section Endpoint /{apiVersion}/workflows/{workflows}/delete:
        #' Delete workflows.
        #' @param study Study [[organization@]project:]study where study and project can be either the ID or UUID.
        #' @param workflows Comma separated list workflow IDs or UUIDs up to a maximum of 100.
        delete=fetchOpenCGA(object=OpencgaR, category="workflows", categoryId=workflows, subcategory=NULL,
                subcategoryId=NULL, action="delete", params=params, httpMethod="DELETE", as.queryParam=NULL, ...),

        #' @section Endpoint /{apiVersion}/workflows/{workflows}/info:
        #' Get workflow information.
        #' @param include Fields included in the response, whole JSON path must be provided.
        #' @param exclude Fields excluded in the response, whole JSON path must be provided.
        #' @param workflows Comma separated list sample IDs or UUIDs up to a maximum of 100.
        #' @param study Study [[organization@]project:]study where study and project can be either the ID or UUID.
        #' @param version Comma separated list of workflow versions. 'all' to get all the workflow versions. Not supported if multiple workflow ids are provided.
        #' @param deleted Boolean to retrieve deleted entries.
        info=fetchOpenCGA(object=OpencgaR, category="workflows", categoryId=workflows, subcategory=NULL,
                subcategoryId=NULL, action="info", params=params, httpMethod="GET", as.queryParam=NULL, ...),
    )
})