
# WARNING: AUTOGENERATED CODE
#
#    This code was generated by a tool.
#    Autogenerated on: 2022-07-01 13:00:18
#    
#    Manual changes to this file may cause unexpected behavior in your application.
#    Manual changes to this file will be overwritten if the code is regenerated.


# ##############################################################################
#' IndividualClient methods
#' @include AllClasses.R
#' @include AllGenerics.R
#' @include commons.R

#' @description This function implements the OpenCGA calls for managing Individuals.

#' The following table summarises the available *actions* for this client:
#'
#' | endpointName | Endpoint WS | parameters accepted |
#' | -- | :-- | --: |
#' | updateAcl | /{apiVersion}/individuals/acl/{members}/update | study, members[*], action[*], propagate, body[*] |
#' | aggregationStats | /{apiVersion}/individuals/aggregationStats | study, hasFather, hasMother, sex, karyotypicSex, ethnicity, population, creationYear, creationMonth, creationDay, creationDayOfWeek, status, lifeStatus, phenotypes, numSamples, parentalConsanguinity, release, version, annotation, default, field |
#' | loadAnnotationSets | /{apiVersion}/individuals/annotationSets/load | study, variableSetId[*], path[*], parents, annotationSetId, body |
#' | create | /{apiVersion}/individuals/create | include, exclude, study, samples, includeResult, body[*] |
#' | distinct | /{apiVersion}/individuals/distinct | study, id, uuid, name, familyIds, father, mother, samples, sex, ethnicity, dateOfBirth, disorders, phenotypes, populationName, populationSubpopulation, karyotypicSex, lifeStatus, internalStatus, status, deleted, creationDate, modificationDate, annotation, acl, release, snapshot, field[*] |
#' | search | /{apiVersion}/individuals/search | include, exclude, limit, skip, count, flattenAnnotations, study, id, uuid, name, father, mother, samples, familyIds, sex, dateOfBirth, ethnicity, disorders, phenotypes, populationName, populationSubpopulation, karyotypicSex, lifeStatus, internalStatus, status, deleted, creationDate, modificationDate, annotation, acl, release, snapshot |
#' | acl | /{apiVersion}/individuals/{individuals}/acl | individuals[*], study, member, silent |
#' | delete | /{apiVersion}/individuals/{individuals}/delete | force, study, individuals[*] |
#' | info | /{apiVersion}/individuals/{individuals}/info | include, exclude, flattenAnnotations, individuals[*], study, version, deleted |
#' | update | /{apiVersion}/individuals/{individuals}/update | include, exclude, individuals[*], study, samplesAction, phenotypesAction, disordersAction, annotationSetsAction, includeResult, body |
#' | updateAnnotationSetsAnnotations | /{apiVersion}/individuals/{individual}/annotationSets/{annotationSet}/annotations/update | individual[*], study, annotationSet[*], action, body |
#' | relatives | /{apiVersion}/individuals/{individual}/relatives | include, exclude, flattenAnnotations, individual[*], study, degree |
#'
#' @md
#' @seealso \url{http://docs.opencb.org/display/opencga/Using+OpenCGA} and the RESTful API documentation
#' \url{http://bioinfo.hpc.cam.ac.uk/opencga-prod/webservices/}
#' [*]: Required parameter
#' @export

setMethod("individualClient", "OpencgaR", function(OpencgaR, members, individuals, annotationSet, individual, endpointName, params=NULL, ...) {
    switch(endpointName,

        #' @section Endpoint /{apiVersion}/individuals/acl/{members}/update:
        #' Update the set of permissions granted for the member.
        #' @param study Study [[user@]project:]study where study and project can be either the ID or UUID.
        #' @param members Comma separated list of user or group ids.
        #' @param action Action to be performed [ADD, SET, REMOVE or RESET].
        #' @param propagate Propagate individual permissions to related samples.
        #' @param data JSON containing the parameters to update the permissions. If propagate flag is set to true, it will propagate the permissions defined to the samples that are associated to the matching individuals.
        updateAcl=fetchOpenCGA(object=OpencgaR, category="individuals", categoryId=NULL, subcategory="acl",
                subcategoryId=members, action="update", params=params, httpMethod="POST", as.queryParam=c("action"),
                ...),

        #' @section Endpoint /{apiVersion}/individuals/aggregationStats:
        #' Fetch catalog individual stats.
        #' @param study Study [[user@]project:]study where study and project can be either the ID or UUID.
        #' @param hasFather Has father.
        #' @param hasMother Has mother.
        #' @param sex Sex.
        #' @param karyotypicSex Karyotypic sex.
        #' @param ethnicity Ethnicity.
        #' @param population Population.
        #' @param creationYear Creation year.
        #' @param creationMonth Creation month (JANUARY, FEBRUARY...).
        #' @param creationDay Creation day.
        #' @param creationDayOfWeek Creation day of week (MONDAY, TUESDAY...).
        #' @param status Status.
        #' @param lifeStatus Life status.
        #' @param phenotypes Phenotypes.
        #' @param numSamples Number of samples.
        #' @param parentalConsanguinity Parental consanguinity.
        #' @param release Release.
        #' @param version Version.
        #' @param annotation Annotation filters. Example: age>30;gender=FEMALE. For more information, please visit http://docs.opencb.org/display/opencga/AnnotationSets+1.4.0.
        #' @param default Calculate default stats.
        #' @param field List of fields separated by semicolons, e.g.: studies;type. For nested fields use >>, e.g.: studies>>biotype;type;numSamples[0..10]:1.
        aggregationStats=fetchOpenCGA(object=OpencgaR, category="individuals", categoryId=NULL, subcategory=NULL,
                subcategoryId=NULL, action="aggregationStats", params=params, httpMethod="GET", as.queryParam=NULL,
                ...),

        #' @section Endpoint /{apiVersion}/individuals/annotationSets/load:
        #' Load annotation sets from a TSV file.
        #' @param study Study [[user@]project:]study where study and project can be either the ID or UUID.
        #' @param variableSetId Variable set ID or name.
        #' @param path Path where the TSV file is located in OpenCGA or where it should be located.
        #' @param parents Flag indicating whether to create parent directories if they don't exist (only when TSV file was not previously associated).
        #' @param annotationSetId Annotation set id. If not provided, variableSetId will be used.
        #' @param data JSON containing the 'content' of the TSV file if this has not yet been registered into OpenCGA.
        loadAnnotationSets=fetchOpenCGA(object=OpencgaR, category="individuals", categoryId=NULL,
                subcategory="annotationSets", subcategoryId=NULL, action="load", params=params, httpMethod="POST",
                as.queryParam=c("variableSetId","path"), ...),

        #' @section Endpoint /{apiVersion}/individuals/create:
        #' Create individual.
        #' @param include Fields included in the response, whole JSON path must be provided.
        #' @param exclude Fields excluded in the response, whole JSON path must be provided.
        #' @param study Study [[user@]project:]study where study and project can be either the ID or UUID.
        #' @param samples Comma separated list of sample ids to be associated to the created individual.
        #' @param includeResult Flag indicating to include the created or updated document result in the response.
        #' @param data JSON containing individual information.
        create=fetchOpenCGA(object=OpencgaR, category="individuals", categoryId=NULL, subcategory=NULL,
                subcategoryId=NULL, action="create", params=params, httpMethod="POST", as.queryParam=NULL, ...),

        #' @section Endpoint /{apiVersion}/individuals/distinct:
        #' Individual distinct method.
        #' @param study Study [[user@]project:]study where study and project can be either the ID or UUID.
        #' @param id Comma separated list individual IDs up to a maximum of 100.
        #' @param uuid Comma separated list individual UUIDs up to a maximum of 100.
        #' @param name Comma separated list individual names up to a maximum of 100.
        #' @param familyIds Comma separated list of family ids the individuals may belong to.
        #' @param father Father ID, name or UUID.
        #' @param mother Mother ID, name or UUID.
        #' @param samples Sample ID, name or UUID.
        #' @param sex Individual sex.
        #' @param ethnicity Individual ethnicity.
        #' @param dateOfBirth Individual date of birth.
        #' @param disorders Comma separated list of disorder ids or names.
        #' @param phenotypes Comma separated list of phenotype ids or names.
        #' @param populationName Population name.
        #' @param populationSubpopulation Subpopulation name.
        #' @param karyotypicSex Individual karyotypic sex.
        #' @param lifeStatus Individual life status.
        #' @param internalStatus Filter by internal status.
        #' @param status Filter by status.
        #' @param deleted Boolean to retrieve deleted entries.
        #' @param creationDate Creation date. Format: yyyyMMddHHmmss. Examples: >2018, 2017-2018, <201805.
        #' @param modificationDate Modification date. Format: yyyyMMddHHmmss. Examples: >2018, 2017-2018, <201805.
        #' @param annotation Annotation filters. Example: age>30;gender=FEMALE. For more information, please visit http://docs.opencb.org/display/opencga/AnnotationSets+1.4.0.
        #' @param acl Filter entries for which a user has the provided permissions. Format: acl={user}:{permissions}. Example: acl=john:WRITE,WRITE_ANNOTATIONS will return all entries for which user john has both WRITE and WRITE_ANNOTATIONS permissions. Only study owners or administrators can query by this field. .
        #' @param release Release when it was created.
        #' @param snapshot Snapshot value (Latest version of the entry in the specified release).
        #' @param field Field for which to obtain the distinct values.
        distinct=fetchOpenCGA(object=OpencgaR, category="individuals", categoryId=NULL, subcategory=NULL,
                subcategoryId=NULL, action="distinct", params=params, httpMethod="GET", as.queryParam=c("field"), ...),

        #' @section Endpoint /{apiVersion}/individuals/search:
        #' Search for individuals.
        #' @param include Fields included in the response, whole JSON path must be provided.
        #' @param exclude Fields excluded in the response, whole JSON path must be provided.
        #' @param limit Number of results to be returned.
        #' @param skip Number of results to skip.
        #' @param count Get the total number of results matching the query. Deactivated by default.
        #' @param flattenAnnotations Flatten the annotations?.
        #' @param study Study [[user@]project:]study where study and project can be either the ID or UUID.
        #' @param id Comma separated list individual IDs up to a maximum of 100.
        #' @param uuid Comma separated list individual UUIDs up to a maximum of 100.
        #' @param name Comma separated list individual names up to a maximum of 100.
        #' @param father Father ID, name or UUID.
        #' @param mother Mother ID, name or UUID.
        #' @param samples Sample ID, name or UUID.
        #' @param familyIds Comma separated list of family ids the individuals may belong to.
        #' @param sex Individual sex.
        #' @param dateOfBirth Individual date of birth.
        #' @param ethnicity Individual ethnicity.
        #' @param disorders Comma separated list of disorder ids or names.
        #' @param phenotypes Comma separated list of phenotype ids or names.
        #' @param populationName Population name.
        #' @param populationSubpopulation Subpopulation name.
        #' @param karyotypicSex Individual karyotypic sex.
        #' @param lifeStatus Individual life status.
        #' @param internalStatus Filter by internal status.
        #' @param status Filter by status.
        #' @param deleted Boolean to retrieve deleted entries.
        #' @param creationDate Creation date. Format: yyyyMMddHHmmss. Examples: >2018, 2017-2018, <201805.
        #' @param modificationDate Modification date. Format: yyyyMMddHHmmss. Examples: >2018, 2017-2018, <201805.
        #' @param annotation Annotation filters. Example: age>30;gender=FEMALE. For more information, please visit http://docs.opencb.org/display/opencga/AnnotationSets+1.4.0.
        #' @param acl Filter entries for which a user has the provided permissions. Format: acl={user}:{permissions}. Example: acl=john:WRITE,WRITE_ANNOTATIONS will return all entries for which user john has both WRITE and WRITE_ANNOTATIONS permissions. Only study owners or administrators can query by this field. .
        #' @param release Release when it was created.
        #' @param snapshot Snapshot value (Latest version of the entry in the specified release).
        search=fetchOpenCGA(object=OpencgaR, category="individuals", categoryId=NULL, subcategory=NULL,
                subcategoryId=NULL, action="search", params=params, httpMethod="GET", as.queryParam=NULL, ...),

        #' @section Endpoint /{apiVersion}/individuals/{individuals}/acl:
        #' Return the acl of the individual. If member is provided, it will only return the acl for the member.
        #' @param individuals Comma separated list of individual IDs, names or UUIDs up to a maximum of 100.
        #' @param study Study [[user@]project:]study where study and project can be either the ID or UUID.
        #' @param member User or group id.
        #' @param silent Boolean to retrieve all possible entries that are queried for, false to raise an exception whenever one of the entries looked for cannot be shown for whichever reason.
        acl=fetchOpenCGA(object=OpencgaR, category="individuals", categoryId=individuals, subcategory=NULL,
                subcategoryId=NULL, action="acl", params=params, httpMethod="GET", as.queryParam=NULL, ...),

        #' @section Endpoint /{apiVersion}/individuals/{individuals}/delete:
        #' Delete existing individuals.
        #' @param force Force the deletion of individuals that already belong to families.
        #' @param study Study [[user@]project:]study where study and project can be either the ID or UUID.
        #' @param individuals Comma separated list of individual ids.
        delete=fetchOpenCGA(object=OpencgaR, category="individuals", categoryId=individuals, subcategory=NULL,
                subcategoryId=NULL, action="delete", params=params, httpMethod="DELETE", as.queryParam=NULL, ...),

        #' @section Endpoint /{apiVersion}/individuals/{individuals}/info:
        #' Get individual information.
        #' @param include Fields included in the response, whole JSON path must be provided.
        #' @param exclude Fields excluded in the response, whole JSON path must be provided.
        #' @param flattenAnnotations Flatten the annotations?.
        #' @param individuals Comma separated list of individual IDs, names or UUIDs up to a maximum of 100.
        #' @param study Study [[user@]project:]study where study and project can be either the ID or UUID.
        #' @param version Comma separated list of individual versions. 'all' to get all the individual versions. Not supported if multiple individual ids are provided.
        #' @param deleted Boolean to retrieve deleted individuals.
        info=fetchOpenCGA(object=OpencgaR, category="individuals", categoryId=individuals, subcategory=NULL,
                subcategoryId=NULL, action="info", params=params, httpMethod="GET", as.queryParam=NULL, ...),

        #' @section Endpoint /{apiVersion}/individuals/{individuals}/update:
        #' Update some individual attributes.
        #' @param include Fields included in the response, whole JSON path must be provided.
        #' @param exclude Fields excluded in the response, whole JSON path must be provided.
        #' @param individuals Comma separated list of individual ids.
        #' @param study Study [[user@]project:]study where study and project can be either the ID or UUID.
        #' @param samplesAction Action to be performed if the array of samples is being updated. Allowed values: ['ADD', 'SET', 'REMOVE']
        #' @param phenotypesAction Action to be performed if the array of phenotypes is being updated [SET, ADD, REMOVE]. Allowed values: ['ADD', 'SET', 'REMOVE']
        #' @param disordersAction Action to be performed if the array of disorders is being updated [SET, ADD, REMOVE]. Allowed values: ['ADD', 'SET', 'REMOVE']
        #' @param annotationSetsAction Action to be performed if the array of annotationSets is being updated. Allowed values: ['ADD', 'SET', 'REMOVE']
        #' @param includeResult Flag indicating to include the created or updated document result in the response.
        #' @param data body.
        update=fetchOpenCGA(object=OpencgaR, category="individuals", categoryId=individuals, subcategory=NULL,
                subcategoryId=NULL, action="update", params=params, httpMethod="POST", as.queryParam=NULL, ...),

        #' @section Endpoint /{apiVersion}/individuals/{individual}/annotationSets/{annotationSet}/annotations/update:
        #' Update annotations from an annotationSet.
        #' @param individual Individual ID, name or UUID.
        #' @param study Study [[user@]project:]study where study and project can be either the ID or UUID.
        #' @param annotationSet AnnotationSet ID to be updated.
        #' @param action Action to be performed: ADD to add new annotations; REPLACE to replace the value of an already existing annotation; SET to set the new list of annotations removing any possible old annotations; REMOVE to remove some annotations; RESET to set some annotations to the default value configured in the corresponding variables of the VariableSet if any. Allowed values: ['ADD', 'SET', 'REMOVE', 'RESET', 'REPLACE']
        #' @param data Json containing the map of annotations when the action is ADD, SET or REPLACE, a json with only the key 'remove' containing the comma separated variables to be removed as a value when the action is REMOVE or a json with only the key 'reset' containing the comma separated variables that will be set to the default value when the action is RESET.
        updateAnnotationSetsAnnotations=fetchOpenCGA(object=OpencgaR, category="individuals", categoryId=individual,
                subcategory="annotationSets", subcategoryId=annotationSet, action="annotations/update", params=params,
                httpMethod="POST", as.queryParam=NULL, ...),

        #' @section Endpoint /{apiVersion}/individuals/{individual}/relatives:
        #' Get individual relatives.
        #' @param include Fields included in the response, whole JSON path must be provided.
        #' @param exclude Fields excluded in the response, whole JSON path must be provided.
        #' @param flattenAnnotations Flatten the annotations?.
        #' @param individual Individual ID, name or UUID.
        #' @param study Study [[user@]project:]study where study and project can be either the ID or UUID.
        #' @param degree Pedigree degree.
        relatives=fetchOpenCGA(object=OpencgaR, category="individuals", categoryId=individual, subcategory=NULL,
                subcategoryId=NULL, action="relatives", params=params, httpMethod="GET", as.queryParam=NULL, ...),
    )
})