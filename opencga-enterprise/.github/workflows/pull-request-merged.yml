name: "Pull Request Merge Workflow delete docker"

on:
  pull_request:
    branches:
      - "develop"
      - "release-*"
    types:
      - closed

jobs:
  call-delete-docker:
    name: Call Reusable Delete Docker Workflow
    uses: ./.github/workflows/reusable-delete-docker.yml
    with:
      task: ${{ github.head_ref }}
    secrets: inherit