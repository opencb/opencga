name: Push OpenCGA TASK

on:
  push:
    branches:
      - TASK-*
  workflow_dispatch:
    inputs:
      branch:
        description: 'Execute workflow'
        default: 'develop'
        required: true

jobs:
  build:
    uses: zetta-genomics/opencga-enterprise/.github/workflows/build-java-enterprise-workflow.yml@$GITHUB_REF_NAME
    with:
      maven_opts: -Dopencga.war.name=opencga

  deploy-docker:
    uses: zetta-genomics/opencga-enterprise/.github/workflows/deploy-docker-hub-enterprise-workflow.yml@TASK-1974
    needs: build
    secrets: inherit
